// ==UserScript==
// @name         YouTube Mass Unliker (Direct Button Click - Aug 2025)
// @namespace    http://tampermonkey.net/
// @version      8.0
// @description  Unlikes all videos in your "Liked Videos" playlist using direct Like button toggle (working as of August 2025) with status box and auto-scrolling.
// @author       ChatGPT
// @match        *://www.youtube.com/*
// @grant        none
// ==/UserScript==

(function () {
    'use strict';

    const DELAY = 2500;
    let processed = new Set();
    let running = false;

    function logBox(text, color = '#0f0') {
        const box = document.getElementById('unlike-box');
        if (!box) return;
        const line = document.createElement('div');
        line.textContent = text;
        line.style.color = color;
        box.appendChild(line);
        box.scrollTop = box.scrollHeight;
    }

    function createLogBox() {
        const box = document.createElement('div');
        box.id = 'unlike-box';
        Object.assign(box.style, {
            position: 'fixed',
            top: '10px',
            right: '10px',
            background: '#111',
            color: '#0f0',
            padding: '10px',
            zIndex: 999999,
            fontSize: '13px',
            fontFamily: 'monospace',
            maxHeight: '250px',
            overflowY: 'auto',
            borderRadius: '8px',
            boxShadow: '0 0 10px rgba(0,255,0,0.4)'
        });
        box.innerHTML = '<b>🟢 YouTube Mass Unliker</b><br>';
        document.body.appendChild(box);
    }

    function isLikedVideosPage() {
        return new URLSearchParams(location.search).get("list") === "LL";
    }

    function scrollDown() {
        window.scrollBy(0, 3000);
    }

    async function sleep(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async function unlikeVisibleVideos() {
        const videoItems = document.querySelectorAll('ytd-playlist-video-renderer');

        for (let item of videoItems) {
            const videoId = item.dataset.videoId;
            const title = item.querySelector('#video-title')?.textContent.trim() || 'Untitled';

            if (processed.has(videoId)) continue;

            const likeBtn = item.querySelector('ytd-toggle-button-renderer button[aria-pressed="true"]');

            if (likeBtn) {
                likeBtn.click();
                processed.add(videoId);
                logBox(`👍 Unliked: ${title}`);
                await sleep(DELAY);
            } else {
                logBox(`⏭️ Skipped or already unliked: ${title}`, '#aaa');
            }
        }
    }

    async function run() {
        if (running) return;
        running = true;

        logBox('✅ Starting to unlike liked videos...');
        while (true) {
            scrollDown();
            await sleep(1000);
            await unlikeVisibleVideos();
        }
    }

    function init() {
        createLogBox();
        const check = setInterval(() => {
            if (isLikedVideosPage()) {
                const items = document.querySelectorAll('ytd-playlist-video-renderer');
                if (items.length > 0) {
                    clearInterval(check);
                    logBox('🎯 Playlist detected. Running script...');
                    run();
                } else {
                    logBox('⌛ Waiting for videos to load...', '#ff0');
                }
            }
        }, 1500);
    }

    window.addEventListener('load', () => {
        init();
    });
})();
